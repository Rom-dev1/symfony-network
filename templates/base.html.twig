<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <header class="">
        <div class="max-w-5xl mx-auto flex py-6 items-center space-x-4">
            <div>
                <h1 class="font-bold text-2xl">The Social Network</h1>
            </div>
            <nav class="flex items-center justify-between grow">
                <div class="flex space-x-4">
                    <div class="flex space-x-2 items-center">
                        <div class="w-[40px] h-[40px]">
                            {% include "svg/home.html.twig" %}
                        </div>
                        <a href="{{path('app_home')}}" class="text-xl">Accueil</a>
                    </div>
                    <div class="flex space-x-2 items-center">
                        {% include "svg/community.html.twig" %}
                        <a href="#" class="text-xl">Membres</a>
                    </div>
                </div>
                <div>
                    <a href="{{path('app_login')}}" class="text-xl bg-blue-400 rounded p-1 text-white">Connection</a>
                    <a href="{{path('app_register')}}" class="text-xl bg-blue-400 rounded p-1 text-white">Inscription</a>
                </div>
            </nav>
        </div>
    </header>
    <body>
        <div class="bg-gray-200 min-h-screen p-8">
            <div class="max-w-5xl mx-auto">
                {% block body %}{% endblock %}
            </div>
        </div>
        <footer class="bg-white p-4">
            <div class="max-w-5xl mx-auto text-center">
                <p>Tout droits réservés &copy; The social Network </p>
            </div>
        </footer>
    </body>
</html>
