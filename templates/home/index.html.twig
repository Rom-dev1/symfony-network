{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}

{% block body %}

{# {% for user in users %}

    {% for publication in user.publications   %}
        {{dump(publication.createdAt)}}
        
            {{dump(publication.content)}}
            <div class="flex">
                <img src="" alt="{{user.avatar}}">
                <span>{{user.firstname}}</span>
            </div>
            <span>{{publication.content}}</span>
        </div>
    {% endfor %}
    
{% endfor %} #}

{% for publication in publications | sort((a , b) => a.createdAt < b.createdAt) %}
    <div class="bg-white my-10 p-4 rounded">
        <div>
            <span>{{publication.user.avatar}}</span>
            <span>{{publication.user.firstname}}</span>
            <span>publié le {{publication.createdAt | date('d-m-Y')}}</span>
        </div>
        <p>{{publication.content}}</p>
    </div>
{% endfor %}

{% endblock %}
